name: CI

on:
    pull_request:
        branches:
            - main

permissions:
    contents: read

jobs:
    lint-and-test:
        uses: sakashita44/.github/.github/workflows/reusable-python-ci.yml@main
